import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { TabsProfesorPage } from './tabs-profesor';

@NgModule({
  declarations: [
    TabsProfesorPage,
  ],
  imports: [
    IonicPageModule.forChild(TabsProfesorPage),
  ],
})
export class TabsProfesorPageModule {}
